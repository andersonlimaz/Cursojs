// = -> atribuição 
// == -> igualdade ignorando o tipo da variavel, fazendo uma conversão implicita 
// === -> igualdade ele diferencia (string, num, Boolean)

const numero = 2;

const numeroPar = (numero % 2) === 0;

console.log(numeroPar)